<style type="text/css">
  .slideshow-image-{{ block.id }} {
    background-position: {{ block.settings.alignment }};
  }

  {% if block.settings.image != blank %}
    @media screen and (max-width: 600px) {
      .slideshow-image-{{ block.id }} {
        {% if block.settings.image_for_mobile != blank %}
          background-image: url('{{ block.settings.image_for_mobile | img_url: '800x' }}');
        {% else %}
          background-image: url('{{ block.settings.image | img_url: '800x' }}');
        {% endif %}
      }
    }

    @media screen and (min-width: 601px) and (max-width: 1024px) {
      .slideshow-image-{{ block.id }} {
        background-image: url('{{ block.settings.image | img_url: '1024x' }}');
      }
    }

    @media screen and (min-width: 1025px) and (max-width: 1600px) {
      .slideshow-image-{{ block.id }} {
        background-image: url('{{ block.settings.image | img_url: '1600x' }}');
      }
    }

    @media screen and (min-width: 1601px) {
      .slideshow-image-{{ block.id }} {
        background-image: url('{{ block.settings.image | img_url: '2048x' }}');
      }
    }
  {% else %}
    .slideshow-image-{{ block.id }} {
      background-image: url('{{ 'dummie-slide.jpg' | asset_url }}');
    }
  {% endif %}
</style>